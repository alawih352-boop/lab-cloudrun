# Xray Cloud Run - Preset Configurations

## 🚀 سريعة البدء مع الإعدادات المعرّفة مسبقاً

بدلاً من إدخال كل الإعدادات يدويًا في كل مرة، يمكنك استخدام أحد الـ Presets المُعرّفة مسبقاً.

---

## 📋 الـ Presets المتاحة

### 1️⃣ Production (الإنتاج)

**للاستخدام على نطاق واسع والحمل العالي**

```bash
Memory: 2048MB
CPU: 1
Max Instances: 16
Concurrency: 100
Timeout: 3600s
```

**معلومات الأداء:**

- 2-3 Mbps لكل مستخدم
- بيئة التنفيذ: الجيل الثاني

**الحالات المناسبة:**

- تطبيقات الإنتاج
- حمل متوازن ومستقر
- عدد متوسط من المستخدمين

---

### 2️⃣ Budget (الميزانية المحدودة)

**للاستخدام الاقتصادي مع أداء جيد**

```bash
Memory: 2048MB
CPU: 2
Max Instances: 8
Concurrency: 1000
```

```
Timeout: 3600s
```

**الحالات المناسبة:**

- تطبيقات صغيرة إلى متوسطة
- موازنة بين الأداء والتكلفة
- عدد محدود من المستخدمين

---

### 3️⃣ Custom (مخصص)

**تحديد كل الإعدادات يدويًا**

استخدم هذا الخيار إذا كان لديك متطلبات خاصة.

---

## 💻 كيفية الاستخدام

### الطريقة 1: التفاعلية (التفاعل مباشر)

```bash
chmod +x deploy-custom.sh
./deploy-custom.sh
```

سيظهر لك:

```
⚡ Quick Start with Presets:
1) production (2048MB, 1 CPU, 16 instances, 100 concurrency)
2) custom (اختياري - configure everything manually)
Select preset [1-2] (default: 1):
```

اختر `1` لاستخدام الـ preset، أو `2` للإعدادات المخصصة.

### الطريقة 2: عبر متغيرات البيئة

```bash
# استخدام preset production
PRESET=production ./deploy-custom.sh

# استخدام preset budget
PRESET=budget ./deploy-custom.sh
```

---

## 🌍 عرض المناطق المتاحة

عند اختيار المنطقة (Region)، سيُعرض لك تلقائياً جميع المناطق المتاحة من Google Cloud Run:

```
🌍 Available Cloud Run Regions:

1) us-central1 (Iowa)
2) us-east1 (South Carolina)
3) us-west1 (Oregon)
4) europe-west1 (Belgium)
5) europe-west6 (Switzerland)
6) asia-northeast1 (Tokyo)
7) asia-southeast1 (Singapore)
8) asia-south1 (Delhi)
9) australia-southeast1 (Sydney)

🌍 Enter region name [us-central1]:
```

**ملاحظة مهمة:** قد تختلف المناطق المتاحة حسب حسابك في Google Cloud. البرنامج سيعرض فقط المناطق النشطة والمتاحة لحسابك.

---

## 📊 مقارنة الـ Presets

| الميزة        | Production      | Custom       |
| ------------- | --------------- | ------------ |
| Memory        | 2048MB          | قابل للتخصيص |
| CPU           | 1               | قابل للتخصيص |
| Max Instances | 16              | قابل للتخصيص |
| Concurrency   | 100             | قابل للتخصيص |
| Timeout       | 3600s           | قابل للتخصيص |
| الأداء        | 2-3 Mbps/مستخدم | متغير        |
| Execution Env | الجيل الثاني    | متغير        |

---

## 💰 تقدير التكاليف

### Production Preset

```
معدل محدود من الطلبات المتزامنة (100)
مناسب للاستخدام الاقتصادي والموازن
توفير أفضل للموارد
```

---

## ⚡ نصائح لاختيار الـ Preset المناسب

1. **ابدأ بـ Budget** إذا كنت تختبر التطبيق للمرة الأولى
2. **انتقل إلى Production** عندما يزيد عدد المستخدمين
3. **استخدم Custom** للمتطلبات الخاصة جداً

---

## 🔧 إضافة Presets جديدة

إذا أردت إضافة preset جديد، عدّل الجزء التالي في `deploy-custom.sh`:

```bash
declare -A PRESETS=(
  [production]="memory=2048|cpu=1|instances=16|concurrency=1000"
  [budget]="memory=2048|cpu=2|instances=8|concurrency=1000"
  [your-preset]="memory=XXX|cpu=X|instances=X|concurrency=X"  # أضف هنا
)
```

ثم أضف حالة جديدة في قسم الاختيار:

```bash
case "$PRESET_CHOICE" in
  1)
    apply_preset "production"
    ;;
  2)
    apply_preset "budget"
    ;;
  3)
    apply_preset "your-preset"  # أضف هنا
    ;;
esac
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تأكد من تثبيت `gcloud` CLI
2. تأكد من تسجيل دخولك: `gcloud auth login`
3. تأكد من اختيار المشروع الصحيح: `gcloud config set project PROJECT_ID`
